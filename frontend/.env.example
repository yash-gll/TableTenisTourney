# Base URL of the backend API.
# Local dev:
VITE_API_URL=http://localhost:8000/api/v1
# Production (Cloud Run) example — set this at build time before `npm run build`:
# VITE_API_URL=https://tt-backend-xxxxxxxx-uc.a.run.app/api/v1
